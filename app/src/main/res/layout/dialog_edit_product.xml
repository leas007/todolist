<?xml version="1.0" encoding="utf-8"?>
<!-- Cambio para ajustar el ancho -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@android:color/white"> <!-- Establece el fondo a blanco -->
    >


    <EditText
        android:id="@+id/editProductName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edit_product_name_hint"
        android:inputType="text"
        android:autofillHints="name"
        android:padding="12dp"
        android:textColor="#000000"
        android:drawableEnd="@drawable/ic_edit_name" /> <!-- Cambia 'ic_edit_name' por el nombre real de tu icono -->
    />


    <EditText
        android:id="@+id/editProductPrice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edit_product_price_hint"
        android:inputType="numberDecimal"
        android:layout_marginTop="16dp"
        android:autofillHints="price"
        android:textColor="@android:color/black"
        android:padding="12dp"
        android:drawableEnd="@drawable/ic_edit_price" /> <!-- Cambia 'ic_edit_price' por el nombre real de tu icono -->
    />

    <!-- ... (el contenido anterior sigue igual) ... -->

    <EditText
        android:id="@+id/editProductDiscount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edit_product_discount_hint"
        android:inputType="numberDecimal"
        android:layout_marginTop="16dp"
        android:drawableEnd="@drawable/ic_edit_discount" /> <!-- Cambia 'ic_edit_discount' por el nombre real de tu icono -->
    />

    <EditText
        android:id="@+id/editProductQuantity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/edit_product_quantity_hint"
        android:inputType="number"
        android:layout_marginTop="16dp"
        android:drawableEnd="@drawable/ic_edit_quantity" /> <!-- Cambia 'ic_edit_quantity' por el nombre real de tu icono -->
    />

    <Spinner
        android:id="@+id/categorySpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:entries="@array/categories"
        android:prompt="@string/category_prompt" />

    <!-- Otros atributos del Spinner -->


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/cancelButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/cancel_button_text"
            android:background="@android:color/darker_gray"
            android:textColor="@android:color/black"
            android:drawableEnd="@drawable/ic_cancel"
            style="?android:attr/buttonBarButtonStyle" />

        <Button
            android:id="@+id/saveButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="@string/save_button_text"
            android:background="@android:color/holo_blue_light"
            android:textColor="@android:color/black"
            android:drawableEnd="@drawable/ic_save"
            style="?android:attr/buttonBarButtonStyle" />

        <Button
            android:id="@+id/deleteInDialogButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="@string/delete_button_text"
            android:background="@android:color/holo_red_light"
            android:textColor="@android:color/black"
            android:drawableEnd="@drawable/ic_delete"
            style="?android:attr/buttonBarButtonStyle"
            />
    </LinearLayout>
</LinearLayout>
